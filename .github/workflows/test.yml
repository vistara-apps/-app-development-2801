name: Test Workflow

on:
  push:
    branches: [ main, master, codegen-bot/complete-prd-implementation-1757274496 ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Test step
      run: echo "✅ Workflow syntax is valid!"
